DB_HOST=mysql
DB_PORT=3306
DB_NAME=gastos
DB_USER=root
DB_PASS=root

JWT_SECRET=gastos-docker-dev-secret
CORS_ORIGIN=http://localhost:4200
APP_URL=http://localhost:4200
APP_TIMEZONE=America/Sao_Paulo
MAIL_DRIVER=log
MAIL_FROM=Tostoes <noreply@tostoes.app>

# Opcional: usuário criado na primeira migração (deixe vazio para usar só /cadastro)
# SETUP_USER=demo
# SETUP_PASSWORD=changeme
# SETUP_NAME=Usuário demo
