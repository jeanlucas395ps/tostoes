# Tostoes, Frontend

App Angular. Documentação e setup completo na **[raiz do repositório](../README.md)**.

```bash
cp .env.example .env
npm install
npm start
```

Requer a API em http://localhost:8090 (`docker compose up -d` na raiz).
